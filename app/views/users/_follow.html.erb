<%= form_for current_user.relationships.
                          build(:followed_id => @user.id) do |f| %>
  <div><%= f.hidden_field :followed_id %></div>
  <button type="button" class="btn btn-success"><%= f.submit "Suivre" %></button>
<% end %>